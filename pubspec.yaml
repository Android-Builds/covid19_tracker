name: covid19_tracker
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons:
  http:
  charts_flutter:
  flutter_svg:
  built_path: 
  built_path_annotations:
  url_launcher:
  flutter_email_sender:
  flutter_launcher_icons:
  shared_preferences:
  share:

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:

flutter_icons:    
 android: "launcher_icon"    
 ios: true    
 image_path: "assets/icon.png"

dependency_overrides:
  analyzer:
  build: 

flutter:
  uses-material-design: true
  assets:
    - assets/

  fonts:
    - family: Telegram
      fonts:
        - asset: icons/Telegram.ttf
